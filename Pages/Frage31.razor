@page "/frage31"
@inject MainService MS
@inject TimeService TS
@using System.ComponentModel.DataAnnotations
@using System.Collections.Generic
@inject NavigationManager Navigation

<h1>Einbürgerungstest - Frage 31</h1>
<br>
    <div class="quiz-container">
    <center><h3 class="question">Was war am 8. Mai 1945?</h3</center>
    <form>
        <div class="btn-container">
           <button class="answer-btn" type="button" @onclick="@(() => FrageFalsch(1))">Tod Adolf Hitlers</button>
          <button class="answer-btn" type="button" @onclick="FrageRichtig">Ende des Zweiten Weltkriegs in Europa</button>
         <button class="answer-btn" type="button" @onclick="@(() => FrageFalsch(2))">Beginn des Berliner Mauerbaus</button>
        <button class="answer-btn" type="button" @onclick="@(() => FrageFalsch(3))">Wahl von Konrad Adenauer zum Bundeskanzler</button>
        </div>
    <br>
<div class="w3-light-grey w3-round">
  <div class="w3-container w3-round w3-blue" style="width:93%">31/33</div>
    </div>
      </form>
    </div>

@code {

private void FrageFalsch(int FrageAuswahl) 
  {
    switch(FrageAuswahl) {
            
        case 1:
    MS.FalscheAntworten.Add("Tod Adolf Hitlers");
    MS.RichtigeAntwortenListe.Add("Ende des Zweiten Weltkriegs in Europa");
    MS.FrageFalsch();
    Navigation.NavigateTo("/frage32");
        break; 

        case 2:
   MS.FrageFalsch();
   MS.FalscheAntworten.Add("Beginn des Berliner Mauerbaus");
   MS.RichtigeAntwortenListe.Add("Ende des Zweiten Weltkriegs in Europa");
   Navigation.NavigateTo("/frage32");
        break;
            
        case 3: 
   MS.FrageFalsch();
   MS.FalscheAntworten.Add("Wahl von Konrad Adenauer zum Bundeskanzler");
   MS.RichtigeAntwortenListe.Add("Ende des Zweiten Weltkriegs in Europa");
   Navigation.NavigateTo("/frage32");
        break; 
    }
  }

    private void FrageRichtig() {
        MS.FrageRichtig();
        Navigation.NavigateTo("/frage32");
    }
} 